---
interface Props {
  href: string;
  target?: string;
  class?: string;
}
const { href, target, class: className } = Astro.props;
---

<a class:list={['btn-pill', className]} href={href} target={target} rel={target === '_blank' ? 'noopener' : undefined}>
  <slot />
</a>
